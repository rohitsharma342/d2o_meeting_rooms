name: d2o_meeting_rooms
description: A streamlined mobile app for managing meeting room bookings

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  table_calendar: ^3.0.9
  intl: ^0.18.1
  flutter_staggered_animations: ^1.1.1
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true